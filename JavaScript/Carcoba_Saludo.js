function greet(nombre, hora, min) {
    if(nombre != "Count Dooku")
        return "Buenos días, "+nombre+". Son las "+hora+":"+min;
    else
        return "¡Voy por ti, Dooku!"
}
console.log(greet("Ramon",11,37));

